{
  "name": "nexora-smart-contracts",
  "version": "1.0.0",
  "description": "NEXORA Web3 Smart Contracts for EVM Payment Settlement",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.17"
  }
}
